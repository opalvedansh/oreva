import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Selected Work — Oréva Digital Agency",
    description: "Explore our portfolio of high-performance digital platforms, e-commerce experiences, and brand systems built for Eraya Wellness, Doree Clothing, Furnimart, Marcella, and more.",
};

export default function PortfolioLayout({ children }: { children: React.ReactNode }) {
    return children;
}
