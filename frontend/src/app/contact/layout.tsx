import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Contact Us — Oréva Digital Agency",
    description: "Ready to build something extraordinary? Reach out to Oréva for a free consultation. Contact us at oreva3685@gmail.com or call +91 77618 65075.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
    return children;
}
